# Client (Vite) environment variables
# Gemini / Google GenAI API key used by client-side services
VITE_GOOGLE_GEMINI_API_KEY=

# Optional fallback (some build configs also read this)
GEMINI_API_KEY=

# URL for API backend (server). Railway will provide a URL in production.
# Use the local server URL for development:
VITE_API_URL=http://localhost:5000

# Google OAuth (optional client-side vars if used by the frontend)
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=
